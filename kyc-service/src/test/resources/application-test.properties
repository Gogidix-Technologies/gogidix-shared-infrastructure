# Test configuration for kyc-service
spring.application.name=kyc-service-test
server.port=0

# Disable config server
spring.cloud.config.enabled=false

# Disable Eureka for tests
eureka.client.enabled=false

# Logging
logging.level.root=WARN
logging.level.com.exalt.ecosystem.infrastructure.kyc=INFO
EOF < /dev/null
