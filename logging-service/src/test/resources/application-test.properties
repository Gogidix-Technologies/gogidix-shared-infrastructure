# Test configuration for logging-service
spring.application.name=logging-service-test
server.port=0

# Disable config server
spring.cloud.config.enabled=false

# Disable Eureka for tests
eureka.client.enabled=false

# Logging
logging.level.root=WARN
logging.level.com.exalt.ecosystem.infrastructure.logging=INFO
EOF < /dev/null
