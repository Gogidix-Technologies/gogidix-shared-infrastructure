console.log("Billing engine service started on port 3002"); const http = require("http"); const server = http.createServer((req, res) => { res.writeHead(200, {"Content-Type": "application/json"}); res.end(JSON.stringify({status: "running", service: "billing-engine", port: 3002})); }); server.listen(3002);
